<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Seagullfly.ViewModels"
             x:Class="Seagullfly.Views.SeagullflyPage"
             x:DataType="viewmodels:SueagullflyPageViewModel"
             Shell.NavBarIsVisible="False">
    <ContentPage.BindingContext>
        <viewmodels:SueagullflyPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid RowDefinitions="*,Auto">
            <Label
                Grid.Row="0"
                Text="{Binding Data}"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand"
                FontSize="Large"/>
            <Button Command="{Binding ScrapDataCommand}" x:Name="ButtonClick" Grid.Row="1" Text="{Binding ButtonName}" />
        </Grid>
    </ContentPage.Content>
</ContentPage>